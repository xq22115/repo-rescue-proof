name: Public PowerShell Variable Command Identity Canary

on:
  push:
    branches:
      - diagnostics/public-standard-hosted-runner-canary-20260822
    paths:
      - '.github/workflows/public-powershell-variable-command-identity-canary.yml'
      - 'diagnostics/powershell-automatic-variable-guard/Test-PowerShell-VariableCommandIdentityCanary.ps1'
  pull_request:
    branches:
      - main
    paths:
      - '.github/workflows/public-powershell-variable-command-identity-canary.yml'
      - 'diagnostics/powershell-automatic-variable-guard/Test-PowerShell-VariableCommandIdentityCanary.ps1'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  native:
    strategy:
      fail-fast: false
      matrix:
        os: [windows-2022, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Checkout exact diagnostic revision
        uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          persist-credentials: false
      - name: Verify exact diagnostic revision
        shell: pwsh
        env:
          EXPECTED_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
        run: |
          $actual = (git rev-parse HEAD).Trim()
          if ($actual -ne $env:EXPECTED_SHA) { throw "VARIABLE_COMMAND_IDENTITY_CANARY_HEAD_MISMATCH expected=$env:EXPECTED_SHA actual=$actual" }
      - name: Probe variable command identity on Windows PowerShell 5.1
        shell: powershell
        run: .\diagnostics\powershell-automatic-variable-guard\Test-PowerShell-VariableCommandIdentityCanary.ps1
      - name: Probe variable command identity on PowerShell 7
        shell: pwsh
        run: .\diagnostics\powershell-automatic-variable-guard\Test-PowerShell-VariableCommandIdentityCanary.ps1
